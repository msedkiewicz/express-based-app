<h2>Contact me</h2>
    {{#if isError}}
    <div class="isError">
    You can't leave fields empty!
    </div>
    {{/if}}
    {{#if isSent}}
    <div class="isSent">
    The message has been sent!
    File {{fileName}} has been saved!
    </div>
    {{/if}}

    {{#if fileName}}
    <div>
        <img src="/uploads/{{fileName}}">
    </div>
    {{/if}}
    <form action="/contact/send-message" method="POST" enctype="multipart/form-data">
        <label>
            Your name: <input type="text" name="author">
        </label>
        <label>
            Your email: <input type="email" name="sender">
        </label>
        <label>
            Title: <input type="text" name="title">
        </label>
        <label>
            File: <input type="file" name="image" accept="image/png, image/gif, image/jpeg, image/jpg"></input>
        </label>
        <label>
            Message: <textarea name="message"></textarea>
        </label>
        <button type="submit">Send message</button>
    </form>